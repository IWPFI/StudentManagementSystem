<UserControl x:Class="StudentManagementSystem.View.TextbookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudentManagementSystem.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Style="{DynamicResource WinBorderEffect}">
        <!--<ItemsControl>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" Rows="2"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>-->
                    <Grid>
                        <Border Width="200" Height="200" Panel.ZIndex="10">
                            <Border.Background>
                                <ImageBrush ImageSource="/Assets/Icon/Border_8.png"/>
                            </Border.Background>
                        </Border>
                        <Border CornerRadius="10" Width="180" Height="195">
                            <Border.Background>
                                <ImageBrush ImageSource="/View/12.jpg"/>
                            </Border.Background>
                        </Border>
                    </Grid>
                <!--</DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>-->
    </Border>
</UserControl>
