<Window x:Class="StudentManagementSystem.View.SearchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentManagementSystem.View"
        mc:Ignorable="d" FontWeight="ExtraLight"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen" 
        WindowStyle="None" AllowsTransparency="True" Background="{x:Null}"
        Title="SearchView" Height="300" Width="600">
    <Border Background="#FF3B4F81" CornerRadius="10" BorderThickness="13" BorderBrush="#FF3B4F81">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBox Grid.Row="1" Style="{StaticResource LookupTextBox}"/>
            <Button Grid.Row="2"  Style="{StaticResource LookupButtonStyle}"/>
        </Grid>
    </Border>
</Window>
